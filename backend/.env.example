# =============================================================================
# Accetraa Technologies — Environment Variables Template
#
# Copy this file to .env and fill in the real values for your environment.
#   cp .env.example .env
#
# IMPORTANT: Never commit .env — it is excluded in .gitignore.
# This file (.env.example) contains ONLY placeholders and IS committed.
# =============================================================================


# =============================================================================
# DJANGO CORE
# =============================================================================

# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=your-50-character-random-secret-key-here

# Settings module to load. Options:
#   accetraa.settings.development   ← local dev
#   accetraa.settings.production    ← production server
DJANGO_SETTINGS_MODULE=accetraa.settings.development

# Set to False in production
DEBUG=True

# Comma-separated. Production example: accetraa.com,www.accetraa.com,api.accetraa.com
ALLOWED_HOSTS=localhost,127.0.0.1


# =============================================================================
# DATABASE — MySQL
# =============================================================================

DB_NAME=accetraa_db
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_HOST=localhost
DB_PORT=3306


# =============================================================================
# CORS — Allowed Frontend Origins
# =============================================================================

# Comma-separated list of frontend origins allowed to call the API.
# Development example: http://localhost:5173,http://localhost:3000
# Production example:  https://accetraa.com,https://www.accetraa.com
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000


# =============================================================================
# EMAIL
# =============================================================================

# Development (prints to console, no real emails sent):
#   django.core.mail.backends.console.EmailBackend
# Production (AWS SES):
#   django_ses.SESBackend
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# The From address used in all outbound emails
EMAIL_FROM=no-reply@accetraa.com

# Internal team email addresses for lead notifications
SALES_EMAIL=sales@accetraa.com
CONTACT_EMAIL=contact@accetraa.com
HR_EMAIL=hr@accetraa.com


# =============================================================================
# AWS — Required in Production only
# =============================================================================
# Leave blank or commented out in development.
# In production, IAM user must have SES send + S3 read/write permissions.

AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# S3 bucket for media file storage (portfolio images, etc.)
AWS_S3_BUCKET_NAME=your-s3-bucket-name
AWS_S3_REGION_NAME=ap-south-1

# CloudFront CDN domain for serving media (optional — omit if not using CloudFront)
AWS_CLOUDFRONT_DOMAIN=cdn.yourdomain.com

# SES region (can differ from S3 region)
AWS_SES_REGION_NAME=ap-south-1
